export default function ProjectRequests({ pendingProjects }) {
    return (
        <div>
            {/* <h4>{pendingProjects.name}</h4>
            <p>Due by: {pendingProjects.dueDate.toDate().toDateString()}</p>
            <span>By: {pendingProjects.createdBy.displayName}</span> */}
        </div>
    )
}
